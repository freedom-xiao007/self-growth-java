# ??
server:
  port: 9000

# sa-token??
sa-token:
  # SSO-????
  sso:
    # SSO-Server? ??????
    auth-url: http://localhost:9000/sso/auth
    # ??????????
    is-slo: true

  # ??Sa-Token?????Redis?? ??????SSO-Server??????Redis?
  alone-redis:
    # Redis????? (???0)
    database: 1
    # Redis?????
    host: 127.0.0.1
    # Redis???????
    port: 6379
    # Redis?????????????
    password: password

spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/self_growth_oauth
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASS:root}